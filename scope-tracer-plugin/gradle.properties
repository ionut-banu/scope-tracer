pluginGroup = com.ionutbanu.scopetracer
pluginVersion = 0.1.0

# IntelliJ Platform target used to compile/run the plugin. JBR (the IDE's own JVM) is
# pinned to JDK 21 across JetBrains products, independent of this version number.
platformType = IC
platformVersion = 2025.3.6.1
pluginSinceBuild = 253

# scope-tracer-analyzer version to bundle, kept in sync by hand with the Maven parent POM
# version (see ../pom.xml). Milestone-1 known rough edge — see plan doc.
analyzerVersion = 0.3.0-SNAPSHOT

gsonVersion = 2.10.1

org.gradle.configuration-cache = true
